# mdfier — Claude instructions

## After approved changes
Once a plan is approved and code changes are made:
1. Stage the changed files (`git add <files>`).
2. Create a commit with a descriptive message summarising what changed and why.
3. `git push` to origin — all without asking for confirmation.
